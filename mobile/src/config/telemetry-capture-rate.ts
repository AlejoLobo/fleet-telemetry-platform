/** Intervalo fijo de captura y sync de respaldo (no configurable desde la UI). */
export const TELEMETRY_CAPTURE_INTERVAL_MILLISECONDS = 5_000;
export const TELEMETRY_SYNC_INTERVAL_MILLISECONDS = 5_000;
